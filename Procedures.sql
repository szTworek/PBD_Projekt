

CREATE PROCEDURE AddStudent(@StudentName varchar(30), @StudentSurname varchar(30), @StudentAddress , @StudentCity , @StudentCountry)
AS

IF NOT EXISTS ( SELECT 1 FROM Adresses WHERE   )

INSERT INTO Students (FirstName, LastName, AddressID) 
VALUES ();
GO;
